<Page xmlns="http://schemas.nativescript.org/tns.xsd"
      loaded="pageLoaded">
      <Page.actionBar>
      <ActionBar title=""/>
  </Page.actionBar>
  <GridLayout rows="auto, *">
    <Label text="HN Top 10" class="title" />

    <ListView items="{{ items }}" itemTap="listViewItemTap" row="1">
      <ListView.itemTemplate>
        <Label text="{{ title }}" class="listItem" textWrap="true"/>
      </ListView.itemTemplate>
    </ListView>
  </GridLayout>
</Page>